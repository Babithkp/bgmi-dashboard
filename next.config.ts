import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['i.pravatar.cc','images.unsplash.com','api.dicebear.com','trikona-bucket.s3.ap-south-1.amazonaws.com'],
  },
};

export default nextConfig;
